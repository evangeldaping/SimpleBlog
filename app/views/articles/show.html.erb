<div class="container">
	 <table class="table table-hover">
    <thead>
      <tr>
        <th> <strong>Title:</strong> </th>
        <th> <strong>Text:</strong>	 </th>
      </tr>
    </thead>
    <tbody>
    	<td><%= @article.title %></td>
    	<td><%= @article.text %></td>
    </tbody>
  </table>

  	  <hr/>
      <h1>Comments</h1>   		
      <td> <%= render @article.comments %> </td>
	
	  <h2>Add a comment:</h2>
	  <td> <%= render 'comments/form' %> </td>
	  <td> <%= link_to 'Edit', edit_article_path(@article) %> | </td>
	  <td> <%= link_to 'Back', articles_path %> </td>  
</div>

